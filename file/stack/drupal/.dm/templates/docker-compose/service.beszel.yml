beszel:
  container_name: ${APPSETTING_SERVICE_BESZEL_HOST}
  hostname: ${APPSETTING_SERVICE_BESZEL_HOST}
  build:
    context: .
    dockerfile: ./templates/dockerfile/beszel
    args:
      APPSETTING_SERVICE_BESZEL_BASE_IMAGE: ${APPSETTING_SERVICE_BESZEL_BASE_IMAGE}
  restart: unless-stopped
  extra_hosts:
    - "host.docker.internal:host-gateway"
  environment:
    APP_URL: ${APPSETTING_SERVICE_BESZEL_APP_URL:-http://${APPSETTING_SERVICE_BESZEL_HOST}:8090}
  volumes:
    - beszel_data:/beszel_data
  # Host port configurable: por defecto 18090 para no chocar con otro Beszel/servicio en 8090.
  ports:
    - "127.0.0.1:${APPSETTING_SERVICE_BESZEL_PUBLISH_PORT:-18090}:8090"
