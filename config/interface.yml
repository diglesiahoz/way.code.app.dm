dm:
  getRunCommands:
  init:
    collection: false
    args:
      name:
        required: true
        type: String
        defaultValue:
      force:
        required: false
        type: Boolean
        defaultValue: false
      remove_all:
        required: false
        type: Boolean
        defaultValue: false
      include:
        required: false
        type: Array
        defaultValue: []
  loadRunEnv:
  main:
  makeCerts:
  purgeAll:
  setExecDbService:
    collection: false
    args:
      base_image:
        required: true
        type: String
        defaultValue:
  setUp: